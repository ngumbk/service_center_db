version: "3.8"
services:
  db_mysql:
    image: mysql:8
    ports:
      - "3306:3306"
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: "123456"
    volumes:
      - ./db_mysql_data:/var/lib/mysql
volumes:
  db_mysql_data:
    external: true